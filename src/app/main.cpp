#include "globals.h"
#include "SurgeSynthesizer.h"
#include <float.h>

#include <iostream>
#include <iomanip>

namespace VSTGUI { void* soHandle = nullptr; }

int main(int argc, char **argv)
{
   std::cout << "Surge App is not yet implemented" << std::endl
             << std::endl
             << "The SurgeApp aspires to be a standalone version of Surge with a GUI and all " << std::endl
             << "but we have not written that code yet. Apologies!" << std::endl
             << std::endl
             << "If you are interested in Surge without a GUI in a standalone executable" << std::endl
             << "please run the Surge 'headless' application and code" << std::endl;
   return 0;
}
