workflow_nightly:
    steps:
    - trigger_services:
        project: home:surge-synth-team
        package: surge-xt-nightly
    filters:
        event: push
        branches:
          only:
            - main
